<Window x:Class="FishBrowser.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="🐟 鱼纹浏览器 - FishBrowser" Height="900" Width="1600" WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow" ResizeMode="CanResizeWithGrip" WindowState="Maximized">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0078D4" Padding="20,0">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                    <TextBlock Text="🐟 鱼纹浏览器 - FishBrowser" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock Text="v1.0" Foreground="White" FontSize="12" Margin="10,0,0,0" VerticalAlignment="Center" Opacity="0.8"/>
                </StackPanel>
                <Button x:Name="TestButton" Content="运行测试" Foreground="White" Background="Transparent" Padding="15,5" Margin="0,0,20,0" HorizontalAlignment="Right" VerticalAlignment="Center" Click="Test_Click" Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar Navigation -->
            <Border Grid.Column="0" Background="#FFFFFF" BorderThickness="0,0,1,0" BorderBrush="#E0E0E0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="0,10,0,0">
                        <Button Content="仪表盘" Style="{StaticResource NavButtonStyle}" Click="Dashboard_Click"/>
                        <Button Content="浏览器管理" Style="{StaticResource NavButtonStyle}" Click="OpenBrowserManagement_Click"/>
                        <Button Content="🎭 Stagehand AI" Style="{StaticResource NavButtonStyle}" Click="StagehandAITask_Click"/>
                        <Button Content="任务管理" Style="{StaticResource NavButtonStyle}" Click="TaskManagement_Click"/>
                        <Button Content="AI 任务" Style="{StaticResource NavButtonStyle}" Click="AITask_Click"/>
                        <Button Content="指纹配置" Style="{StaticResource NavButtonStyle}" Click="FingerprintConfig_Click"/>
                        <Button Content="代理池" Style="{StaticResource NavButtonStyle}" Click="ProxyPool_Click"/>
                        <Button Content="AI 分析" Style="{StaticResource NavButtonStyle}" Click="AIResults_Click"/>
                        <Button Content="日志" Style="{StaticResource NavButtonStyle}" Click="Logs_Click"/>
                        <Button Content="设置" Style="{StaticResource NavButtonStyle}" Click="Settings_Click"/>
                        <Separator Margin="10,10,10,10" Background="#E0E0E0"/>
                        <TextBlock Text="AI 配置" Margin="16,6,0,6" Foreground="#444" FontWeight="Bold"/>
                        <Button Content="AI 提供商" Style="{StaticResource NavButtonStyle}" Click="AIProviderConfig_Click"/>
                        <Separator Margin="10,10,10,10" Background="#E0E0E0"/>
                        <TextBlock Text="元数据管理" Margin="16,6,0,6" Foreground="#444" FontWeight="Bold"/>
                        <Button Content="预设管理" Style="{StaticResource NavButtonStyle}" Click="OpenPresetManagement_Click"/>
                        <Button Content="元配置编辑器" Style="{StaticResource NavButtonStyle}" Click="OpenMetaEditor_Click"/>
                        <Button Content="元目录浏览" Style="{StaticResource NavButtonStyle}" Click="OpenTraitCatalog_Click"/>
                        <Button Content="批量生成" Style="{StaticResource NavButtonStyle}" Click="OpenBatchGeneration_Click"/>
                        <Button Content="导入目录 (JSON)" Style="{StaticResource NavButtonStyle}" Click="ImportCatalog_Click"/>
                        <Button Content="导出目录 (JSON)" Style="{StaticResource NavButtonStyle}" Click="ExportCatalog_Click"/>
                        <Button Content="帮助" Style="{StaticResource NavButtonStyle}" Click="Help_Click"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Main Panel -->
            <Border Grid.Column="1" Background="#FFFFFF" Margin="5">
                <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
            </Border>
        </Grid>

    </Grid>
</Window>
