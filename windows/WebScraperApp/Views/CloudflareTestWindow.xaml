<Window x:Class="WebScraperApp.Views.CloudflareTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Cloudflare 绕过测试" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" FontSize="20" FontWeight="Bold" Margin="0,0,0,20">
            🔒 Cloudflare Turnstile 绕过测试
        </TextBlock>

        <!-- 配置区域 -->
        <GroupBox Grid.Row="1" Header="浏览器配置" Margin="0,0,0,10">
            <StackPanel Margin="10">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="测试 URL:" Width="100" VerticalAlignment="Center"/>
                    <TextBox x:Name="UrlTextBox" Width="400" Text="https://m.iyf.tv/" VerticalAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="平台:" Width="100" VerticalAlignment="Center"/>
                    <ComboBox x:Name="PlatformComboBox" Width="200" SelectedIndex="0">
                        <ComboBoxItem Content="iPhone (iOS)"/>
                        <ComboBoxItem Content="iPad (iOS)"/>
                        <ComboBoxItem Content="Win32 (Windows)"/>
                        <ComboBoxItem Content="MacIntel (macOS)"/>
                        <ComboBoxItem Content="Linux armv8l (Android)"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="User-Agent:" Width="100" VerticalAlignment="Top" Margin="0,5,0,0"/>
                    <TextBox x:Name="UserAgentTextBox" Width="600" Height="60" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                             Text="Mozilla/5.0 (iPhone; CPU iPhone OS 17_0 like Mac OS X) AppleWebKit/605.1.15 (KHTML, like Gecko) Version/17.0 Mobile/15E148 Safari/604.1"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <!-- 操作按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="LaunchButton" Content="🚀 启动浏览器 (C#)" Width="180" Height="40" 
                    FontSize="14" FontWeight="Bold" Click="LaunchButton_Click" Margin="0,0,10,0"
                    ToolTip="使用 C# Selenium 启动浏览器"/>
            <Button x:Name="PythonServiceButton" Content="🐍 Python 服务" Width="150" Height="40" 
                    FontSize="14" FontWeight="Bold" Click="PythonServiceButton_Click" Margin="0,0,10,0"
                    Background="#4CAF50" Foreground="White" BorderThickness="0"
                    ToolTip="使用 Python undetected-chromedriver (推荐)"/>
            <Button x:Name="StopButton" Content="⏹ 停止" Width="100" Height="40" 
                    Click="StopButton_Click" IsEnabled="False" Margin="0,0,10,0"/>
            <Button x:Name="ClearLogButton" Content="🗑 清空日志" Width="100" Height="40" 
                    Click="ClearLogButton_Click"/>
        </StackPanel>

        <!-- 日志区域 -->
        <GroupBox Grid.Row="3" Header="运行日志">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="LogTextBox" IsReadOnly="True" TextWrapping="Wrap" 
                         FontFamily="Consolas" FontSize="12" Background="#1E1E1E" Foreground="#D4D4D4"
                         VerticalScrollBarVisibility="Auto"/>
            </ScrollViewer>
        </GroupBox>

        <!-- 状态栏 -->
        <StatusBar Grid.Row="4" Height="30" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock x:Name="StatusTextBlock" Text="就绪" FontWeight="Bold"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
