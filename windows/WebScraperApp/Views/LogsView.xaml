<Page x:Class="FishBrowser.WPF.Views.LogsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="日志">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="系统日志" FontSize="24" FontWeight="Bold" Margin="20,20,20,0"/>

        <!-- 工具栏 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="20,20,20,0">
            <Button x:Name="RefreshButton" Content="刷新" Background="#0078D4" Foreground="White" Padding="15,8" Cursor="Hand" Click="Refresh_Click"/>
            <Button x:Name="ClearButton" Content="清空日志" Background="#F0F0F0" Padding="15,8" Margin="10,0,0,0" Cursor="Hand" Click="Clear_Click"/>
            <Button x:Name="ExportButton" Content="导出" Background="#F0F0F0" Padding="15,8" Margin="10,0,0,0" Cursor="Hand" Click="Export_Click"/>
            
            <!-- 日志级别过滤 -->
            <TextBlock Text="级别:" Margin="30,0,10,0" VerticalAlignment="Center" Foreground="#666666"/>
            <ComboBox x:Name="LevelFilter" Width="100" Margin="0,0,10,0">
                <ComboBoxItem>全部</ComboBoxItem>
                <ComboBoxItem>INFO</ComboBoxItem>
                <ComboBoxItem>WARN</ComboBoxItem>
                <ComboBoxItem>ERROR</ComboBoxItem>
            </ComboBox>
        </StackPanel>

        <!-- 日志列表 -->
        <DataGrid Grid.Row="2" x:Name="LogDataGrid" AutoGenerateColumns="False" Background="White" BorderThickness="1" BorderBrush="#E0E0E0" Margin="20,20,20,20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="时间" Binding="{Binding Timestamp, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="150"/>
                <DataGridTextColumn Header="级别" Binding="{Binding Level}" Width="80"/>
                <DataGridTextColumn Header="来源" Binding="{Binding Source}" Width="100"/>
                <DataGridTextColumn Header="消息" Binding="{Binding Message}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
