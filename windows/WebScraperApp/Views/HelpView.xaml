<Page x:Class="FishBrowser.WPF.Views.HelpView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="帮助中心">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <Border Grid.Row="0" Background="#0078D4" Padding="20,15">
            <TextBlock Text="帮助中心" FontSize="24" FontWeight="Bold" Foreground="White"/>
        </Border>

        <!-- 内容区域 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30,20">
                
                <!-- Playwright 安装指南 -->
                <Border Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="🔧" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox Text="Playwright 浏览器管理" FontSize="18" FontWeight="Bold" Foreground="#333333"
                                   Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                   TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="25"
                                   ToolTip="点击选择文字进行复制"/>
                        </StackPanel>
                        
                        <!-- 状态检查 -->
                        <Border Background="#E8F5E8" CornerRadius="6" Padding="15" Margin="0,0,0,15">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBlock Text="📊" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBox Text="安装状态" FontSize="14" FontWeight="Bold" Foreground="#388E3C"
                                   Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                   TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                   ToolTip="点击选择文字进行复制"/>
                                    <Button x:Name="RefreshStatusButton" Content="刷新状态" 
                                           Background="#4CAF50" Foreground="White" Padding="10,5" 
                                           FontSize="12" Margin="20,0,0,0" Click="RefreshStatus_Click"/>
                                </StackPanel>
                                
                                <Grid x:Name="StatusGrid">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <TextBox Text="安装状态:" Grid.Row="0" Grid.Column="0" FontSize="12" FontWeight="Bold" Foreground="#555555" Margin="0,0,0,5"
                                   Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                   TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                   ToolTip="点击选择文字进行复制"/>
                                    <TextBox x:Name="InstallStatusText" Text="检查中..." Grid.Row="0" Grid.Column="1" FontSize="12" Foreground="#666666" Margin="0,0,0,5"
                                   Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                   TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                   ToolTip="点击选择文字进行复制"/>
                                    
                                    <TextBox Text="版本信息:" Grid.Row="1" Grid.Column="0" FontSize="12" FontWeight="Bold" Foreground="#555555" Margin="0,5,0,5"
                                   Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                   TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                   ToolTip="点击选择文字进行复制"/>
                                    <TextBox x:Name="VersionInfoText" Text="检查中..." Grid.Row="1" Grid.Column="1" FontSize="12" Foreground="#666666" Margin="0,5,0,5"
                                   Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                   TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                   ToolTip="点击选择文字进行复制"/>
                                    
                                    <TextBox Text="安装路径:" Grid.Row="2" Grid.Column="0" FontSize="12" FontWeight="Bold" Foreground="#555555" Margin="0,5,0,5"
                                   Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                   TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                   ToolTip="点击选择文字进行复制"/>
                                    <TextBox x:Name="InstallPathText" Text="检查中..." Grid.Row="2" Grid.Column="1" FontSize="12" Foreground="#666666" Margin="0,5,0,5"
                                   Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                   TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                   ToolTip="点击选择文字进行复制"/>
                                    
                                    <TextBox Text="浏览器列表:" Grid.Row="3" Grid.Column="0" FontSize="12" FontWeight="Bold" Foreground="#555555" Margin="0,5,0,5"
                                   Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                   TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                   ToolTip="点击选择文字进行复制"/>
                                    <TextBox x:Name="BrowserListText" Text="检查中..." Grid.Row="3" Grid.Column="1" FontSize="12" Foreground="#666666" Margin="0,5,0,5"
                                   Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                   TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                   ToolTip="点击选择文字进行复制"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                        
                        <!-- 进度条 -->
                        <Border x:Name="ProgressBorder" Background="#FFF3E0" CornerRadius="6" Padding="15" Margin="0,0,0,15" Visibility="Collapsed">
                            <StackPanel>
                                <TextBox Text="📈 安装进度" FontSize="14" FontWeight="Bold" Foreground="#F57C00" Margin="0,0,0,10"
                                   Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                   TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                   ToolTip="点击选择文字进行复制"/>
                                <TextBox x:Name="ProgressText" Text="准备安装..." FontSize="12" Foreground="#666666" Margin="0,0,0,5"
                                   Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                   TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                   ToolTip="点击选择文字进行复制"/>
                                <ProgressBar x:Name="InstallProgressBar" Height="20" Minimum="0" Maximum="100" Value="0" 
                                            Background="#E0E0E0" Foreground="#F57C00" Margin="0,0,0,5"/>
                                <TextBox x:Name="ProgressDetailText" Text="" FontSize="11" Foreground="#888888"
                                   Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                   TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                   ToolTip="点击选择文字进行复制"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- 操作按钮 -->
                        <Border Background="#E3F2FD" CornerRadius="6" Padding="15" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBox Text="🚀 安装操作" FontSize="14" FontWeight="Bold" Foreground="#1976D2" Margin="0,0,0,10"
                                   Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                   TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                   ToolTip="点击选择文字进行复制"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Button x:Name="InstallPlaywrightButton" Content="安装" Grid.Column="0"
                                           Background="#1976D2" Foreground="White" Padding="15,8" 
                                           FontSize="13" FontWeight="Bold" Cursor="Hand" Click="InstallPlaywright_Click" Margin="0,0,5,0"/>
                                    
                                    <Button x:Name="UpdatePlaywrightButton" Content="更新" Grid.Column="1"
                                           Background="#FF9800" Foreground="White" Padding="15,8" 
                                           FontSize="13" FontWeight="Bold" Cursor="Hand" Click="UpdatePlaywright_Click" Margin="5,0"/>
                                    
                                    <Button x:Name="UninstallPlaywrightButton" Content="卸载" Grid.Column="2"
                                           Background="#F44336" Foreground="White" Padding="15,8" 
                                           FontSize="13" FontWeight="Bold" Cursor="Hand" Click="UninstallPlaywright_Click" Margin="5,0,0,0"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                        
                        <!-- 方法 2: 手动安装 -->
                        <Border Background="#FFF3E0" CornerRadius="6" Padding="15" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="📝 方法 2: 手动安装" FontSize="14" FontWeight="Bold" Foreground="#F57C00" Margin="0,0,0,10"/>
                                <TextBox Text="如果一键安装失败，请手动执行以下命令：" FontSize="13" Foreground="#555555" Margin="0,0,0,10"
                                   Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                   TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                   ToolTip="点击选择文字进行复制"/>
                                <Border Background="#263238" CornerRadius="4" Padding="10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="Command1TextBox" Text="pwsh bin/Debug/net9.0-windows/playwright.ps1 install" 
                                                 FontFamily="Consolas" FontSize="12" Foreground="#4CAF50"
                                                 Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                                 TextWrapping="NoWrap" VerticalAlignment="Center"
                                                 ToolTip="点击选择文字" GotFocus="CommandTextBox_GotFocus"/>
                                        <Button x:Name="CopyCommand1Button" Grid.Column="1" 
                                               Background="Transparent" BorderThickness="0" 
                                               Foreground="#4CAF50" Padding="8,4" Margin="5,0,0,0"
                                               ToolTip="复制命令" Click="CopyCommand1_Click">
                                            <TextBlock Text="📋" FontSize="14"/>
                                        </Button>
                                    </Grid>
                                </Border>
                                <TextBox Text="或在项目目录下运行：" FontSize="13" Foreground="#555555" Margin="0,10,0,5"
                                   Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                   TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                   ToolTip="点击选择文字进行复制"/>
                                <Border Background="#263238" CornerRadius="4" Padding="10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="Command2TextBox" Text="playwright install" 
                                                 FontFamily="Consolas" FontSize="12" Foreground="#4CAF50"
                                                 Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                                 TextWrapping="NoWrap" VerticalAlignment="Center"
                                                 ToolTip="点击选择文字" GotFocus="CommandTextBox_GotFocus"/>
                                        <Button x:Name="CopyCommand2Button" Grid.Column="1" 
                                               Background="Transparent" BorderThickness="0" 
                                               Foreground="#4CAF50" Padding="8,4" Margin="5,0,0,0"
                                               ToolTip="复制命令" Click="CopyCommand2_Click">
                                            <TextBlock Text="📋" FontSize="14"/>
                                        </Button>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </Border>
                        
                        <!-- 验证安装 -->
                        <Border Background="#E8F5E8" CornerRadius="6" Padding="15">
                            <StackPanel>
                                <TextBlock Text="✅ 验证安装" FontSize="14" FontWeight="Bold" Foreground="#388E3C" Margin="0,0,0,10"/>
                                <TextBox Text="安装完成后，重新运行应用并点击[运行测试]按钮验证。" FontSize="13" Foreground="#555556"
                                   Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                   TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                   ToolTip="点击选择文字进行复制"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- 常见问题 -->
                <Border Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="❓" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="常见问题" FontSize="18" FontWeight="Bold" Foreground="#333333"/>
                        </StackPanel>
                        
                        <StackPanel>
                            <TextBox Text="Q: 安装失败怎么办？" FontSize="14" FontWeight="Bold" Foreground="#555555" Margin="0,0,0,5"
                                     Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                     TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                     ToolTip="点击选择文字进行复制"/>
                            <TextBox Text="A: 请检查网络连接，或以管理员身份运行应用。" FontSize="13" Foreground="#666666" Margin="0,0,0,15"
                                     Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                     TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                     ToolTip="点击选择文字进行复制"/>
                            
                            <TextBox Text="Q: 安装需要多长时间？" FontSize="14" FontWeight="Bold" Foreground="#555555" Margin="0,0,0,5"
                                     Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                     TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                     ToolTip="点击选择文字进行复制"/>
                            <TextBox Text="A: 首次安装需要下载约 200-300MB，通常需要 5-10 分钟。" FontSize="13" Foreground="#666666" Margin="0,0,0,15"
                                     Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                     TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                     ToolTip="点击选择文字进行复制"/>
                            
                            <TextBox Text="Q: 安装位置在哪里？" FontSize="14" FontWeight="Bold" Foreground="#555555" Margin="0,0,0,5"
                                     Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                     TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                     ToolTip="点击选择文字进行复制"/>
                            <TextBox Text="A: C:\Users\[用户名]\AppData\Local\ms-playwright\" FontSize="13" Foreground="#666666"
                                     Background="Transparent" BorderThickness="0" IsReadOnly="True"
                                     TextWrapping="Wrap" VerticalAlignment="Stretch" MinHeight="20"
                                     ToolTip="点击选择文字进行复制"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 系统信息 -->
                <Border Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="ℹ️" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="系统信息" FontSize="18" FontWeight="Bold" Foreground="#333333"/>
                        </StackPanel>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Text="应用版本：" Grid.Row="0" Grid.Column="0" FontSize="13" FontWeight="Bold" Foreground="#555555" VerticalAlignment="Center"/>
                            <TextBlock x:Name="VersionText" Text="v1.0" Grid.Row="0" Grid.Column="1" FontSize="13" Foreground="#666666" VerticalAlignment="Center"/>
                            
                            <TextBlock Text="框架版本：" Grid.Row="1" Grid.Column="0" FontSize="13" FontWeight="Bold" Foreground="#555555" VerticalAlignment="Center" Margin="0,10,0,0"/>
                            <TextBlock Text=".NET 9.0" Grid.Row="1" Grid.Column="1" FontSize="13" Foreground="#666666" VerticalAlignment="Center" Margin="0,10,0,0"/>
                            
                            <TextBlock Text="日志位置：" Grid.Row="2" Grid.Column="0" FontSize="13" FontWeight="Bold" Foreground="#555555" VerticalAlignment="Center" Margin="0,10,0,0"/>
                            <TextBlock x:Name="LogPathText" Text="logs/webscraper.log" Grid.Row="2" Grid.Column="1" FontSize="13" Foreground="#666666" VerticalAlignment="Center" Margin="0,10,0,0"/>
                            
                            <TextBlock Text="数据库位置：" Grid.Row="3" Grid.Column="0" FontSize="13" FontWeight="Bold" Foreground="#555555" VerticalAlignment="Center" Margin="0,10,0,0"/>
                            <TextBlock Text="webscraper.db" Grid.Row="3" Grid.Column="1" FontSize="13" Foreground="#666666" VerticalAlignment="Center" Margin="0,10,0,0"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
