<Window x:Class="FishBrowser.WPF.Views.Dialogs.AIProviderQuickSetupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="快速配置 AI 提供商" Height="600" Width="800"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- 提供商卡片样式 -->
        <Style x:Key="ProviderCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Height" Value="140"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#0078D4"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#0078D4" Opacity="0.3" BlurRadius="10" ShadowDepth="0"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 选中的卡片样式 -->
        <Style x:Key="SelectedCardStyle" TargetType="Border" BasedOn="{StaticResource ProviderCardStyle}">
            <Setter Property="BorderBrush" Value="#0078D4"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#0078D4" Opacity="0.4" BlurRadius="12" ShadowDepth="0"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 步骤指示器 -->
        <Border Grid.Row="0" Background="White" BorderThickness="0,0,0,1" BorderBrush="#E0E0E0" Padding="24,20">
            <StackPanel>
                <TextBlock x:Name="StepTitle" Text="步骤 1/3：选择 AI 提供商" FontSize="20" FontWeight="Bold"/>
                <TextBlock x:Name="StepDescription" Text="选择你想使用的 AI 服务提供商" 
                           FontSize="13" Foreground="#666" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- 内容区 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24,20">
            <StackPanel>
                <!-- 步骤 1: 选择提供商 -->
                <StackPanel x:Name="Step1Panel" Visibility="Visible">
                    <TextBlock Text="🌍 国际提供商" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <WrapPanel Margin="0,0,0,24">
                        <Border Style="{StaticResource ProviderCardStyle}" MouseLeftButtonDown="SelectProvider_Click" Tag="OpenAI">
                            <StackPanel>
                                <TextBlock Text="🤖" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock Text="OpenAI" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="GPT-4 / GPT-3.5" FontSize="11" Foreground="#666" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                <TextBlock Text="最强大，适合复杂任务" FontSize="10" Foreground="#999" HorizontalAlignment="Center" Margin="0,4,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource ProviderCardStyle}" MouseLeftButtonDown="SelectProvider_Click" Tag="GoogleGemini">
                            <StackPanel>
                                <TextBlock Text="🌟" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock Text="Google Gemini" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="Gemini Pro" FontSize="11" Foreground="#666" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                <TextBlock Text="免费额度大" FontSize="10" Foreground="#999" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource ProviderCardStyle}" MouseLeftButtonDown="SelectProvider_Click" Tag="AnthropicClaude">
                            <StackPanel>
                                <TextBlock Text="🧠" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock Text="Claude" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="Claude 3" FontSize="11" Foreground="#666" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                <TextBlock Text="长上下文，安全性高" FontSize="10" Foreground="#999" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </WrapPanel>

                    <TextBlock Text="🇨🇳 国内提供商" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <WrapPanel Margin="0,0,0,24">
                        <Border Style="{StaticResource ProviderCardStyle}" MouseLeftButtonDown="SelectProvider_Click" Tag="AlibabaQwen">
                            <StackPanel>
                                <TextBlock Text="☁️" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock Text="通义千问" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="阿里云" FontSize="11" Foreground="#666" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                <TextBlock Text="中文优秀，性价比高" FontSize="10" Foreground="#999" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource ProviderCardStyle}" MouseLeftButtonDown="SelectProvider_Click" Tag="ModelScope">
                            <StackPanel>
                                <TextBlock Text="🪄" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock Text="魔塔社区" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="ModelScope" FontSize="11" Foreground="#666" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                <TextBlock Text="开源模型，免费使用" FontSize="10" Foreground="#999" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource ProviderCardStyle}" MouseLeftButtonDown="SelectProvider_Click" Tag="SiliconFlow">
                            <StackPanel>
                                <TextBlock Text="⚡" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock Text="硅基流动" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="SiliconFlow" FontSize="11" Foreground="#666" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                <TextBlock Text="高性能推理，按量计费" FontSize="10" Foreground="#999" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource ProviderCardStyle}" MouseLeftButtonDown="SelectProvider_Click" Tag="ZhipuGLM">
                            <StackPanel>
                                <TextBlock Text="🎓" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock Text="智谱 GLM" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="GLM-4" FontSize="11" Foreground="#666" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                <TextBlock Text="清华背景，中文强" FontSize="10" Foreground="#999" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource ProviderCardStyle}" MouseLeftButtonDown="SelectProvider_Click" Tag="MoonshotAI">
                            <StackPanel>
                                <TextBlock Text="🌙" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock Text="Moonshot" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="Kimi" FontSize="11" Foreground="#666" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                <TextBlock Text="超长上下文 128K" FontSize="10" Foreground="#999" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </WrapPanel>

                    <TextBlock Text="💻 本地部署" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <WrapPanel>
                        <Border Style="{StaticResource ProviderCardStyle}" MouseLeftButtonDown="SelectProvider_Click" Tag="Ollama">
                            <StackPanel>
                                <TextBlock Text="🦙" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock Text="Ollama" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="本地模型" FontSize="11" Foreground="#666" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                <TextBlock Text="完全免费，数据安全" FontSize="10" Foreground="#999" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </WrapPanel>
                </StackPanel>

                <!-- 步骤 2: 选择模型 -->
                <StackPanel x:Name="Step2Panel" Visibility="Collapsed">
                    <TextBlock x:Name="SelectedProviderText" Text="已选择：OpenAI" FontSize="14" Foreground="#666" Margin="0,0,0,16"/>
                    
                    <TextBlock Text="选择模型" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <ItemsControl x:Name="ModelsList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                                        CornerRadius="6" Padding="16" Margin="0,0,0,12" Cursor="Hand"
                                        MouseLeftButtonDown="SelectModel_Click" Tag="{Binding ModelId}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <RadioButton x:Name="ModelRadio" Grid.Column="0" VerticalAlignment="Center" 
                                                     Margin="0,0,12,0" IsChecked="{Binding IsSelected}"/>

                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding DisplayName}" FontSize="14" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#666" Margin="0,4,0,0"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                                            <TextBlock Text="{Binding PriceDisplay}" FontSize="11" Foreground="#999"/>
                                            <TextBlock Text="{Binding ContextDisplay}" FontSize="11" Foreground="#999" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <!-- 步骤 3: 输入 API Key -->
                <StackPanel x:Name="Step3Panel" Visibility="Collapsed">
                    <TextBlock x:Name="Step3ProviderText" Text="配置：OpenAI GPT-4" FontSize="14" Foreground="#666" Margin="0,0,0,16"/>

                    <Border Background="#FFF9E6" BorderBrush="#FFE5B3" BorderThickness="1" CornerRadius="6" Padding="16" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="💡 如何获取 API Key？" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <TextBlock x:Name="ApiKeyHelpText" TextWrapping="Wrap" FontSize="12" Foreground="#666"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="API 密钥" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="ApiKeyTextBox" Grid.Column="0" Padding="12,10" FontSize="13"
                             BorderBrush="#E0E0E0" BorderThickness="1" TextChanged="ApiKeyTextBox_TextChanged"/>
                        <Button Grid.Column="1" Content="📋 粘贴" Padding="12,10" Margin="8,0,0,0"
                                Click="PasteApiKey_Click" Background="White" BorderBrush="#E0E0E0" BorderThickness="1"/>
                    </Grid>

                    <TextBlock Text="配置名称（可选）" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <TextBox x:Name="ConfigNameTextBox" Padding="12,10" FontSize="13" Margin="0,0,0,16"
                             BorderBrush="#E0E0E0" BorderThickness="1"/>

                    <CheckBox x:Name="AddMoreKeysCheckBox" Content="稍后添加更多密钥（用于轮询）" 
                              FontSize="12" Foreground="#666" Margin="0,0,0,16"/>

                    <Border Background="#E8F5E9" BorderBrush="#4CAF50" BorderThickness="1" CornerRadius="6" Padding="16">
                        <StackPanel>
                            <TextBlock Text="✅ 配置完成后" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <TextBlock Text="• 系统会自动测试连接" FontSize="12" Foreground="#666"/>
                            <TextBlock Text="• 可在「AI 任务」中使用" FontSize="12" Foreground="#666" Margin="0,4,0,0"/>
                            <TextBlock Text="• 可随时添加更多密钥" FontSize="12" Foreground="#666" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- 底部按钮 -->
        <Border Grid.Row="2" Background="White" BorderThickness="0,1,0,0" BorderBrush="#E0E0E0" Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="BackButton" Content="← 上一步" Padding="20,10" 
                        Click="Back_Click" Visibility="Collapsed"
                        Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="取消" Padding="20,10" Margin="0,0,12,0" Click="Cancel_Click"
                            Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand"/>
                    <Button x:Name="NextButton" Content="下一步 →" Padding="20,10" Click="Next_Click"
                            Background="#0078D4" Foreground="White" BorderThickness="0" 
                            FontWeight="SemiBold" Cursor="Hand" IsEnabled="False"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
