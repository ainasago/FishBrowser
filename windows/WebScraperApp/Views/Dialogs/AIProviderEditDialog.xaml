<Window x:Class="FishBrowser.WPF.Views.Dialogs.AIProviderEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="编辑 AI 提供商" Height="700" Width="900"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="White" BorderThickness="0,0,0,1" BorderBrush="#E0E0E0" Padding="24,20">
            <StackPanel>
                <TextBlock Text="编辑 AI 提供商配置" FontSize="20" FontWeight="Bold"/>
                <TextBlock Text="修改提供商设置和 API 密钥" FontSize="13" Foreground="#666" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- 内容区 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24,20">
            <StackPanel>
                <!-- 基本信息 -->
                <TextBlock Text="基本信息" Style="{StaticResource SectionTitleStyle}"/>
                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6" Padding="20" Margin="0,0,0,20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="0,0,0,16">
                            <TextBlock Text="配置名称" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="NameTextBox" Style="{StaticResource TextBoxStyle}"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Margin="0,0,0,16">
                            <TextBlock Text="提供商类型" Style="{StaticResource LabelStyle}"/>
                            <ComboBox x:Name="ProviderTypeComboBox" Style="{StaticResource ComboBoxStyle}" 
                                      SelectionChanged="ProviderType_SelectionChanged"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Margin="0,0,0,16">
                            <TextBlock Text="模型" Style="{StaticResource LabelStyle}"/>
                            <ComboBox x:Name="ModelComboBox" Style="{StaticResource ComboBoxStyle}"/>
                        </StackPanel>

                        <StackPanel Grid.Row="3">
                            <TextBlock Text="API 地址" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="BaseUrlTextBox" Style="{StaticResource TextBoxStyle}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- API 密钥 -->
                <TextBlock Text="API 密钥" Style="{StaticResource SectionTitleStyle}"/>
                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="支持多个密钥，系统会自动轮询使用" FontSize="12" Foreground="#666" VerticalAlignment="Center"/>
                            <Button Content="➕ 添加密钥" Padding="12,6" HorizontalAlignment="Right" 
                                    Click="AddApiKey_Click" Background="#0078D4" Foreground="White" 
                                    BorderThickness="0" Cursor="Hand"/>
                        </Grid>

                        <ItemsControl x:Name="ApiKeysList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="1" 
                                            CornerRadius="4" Padding="16" Margin="0,0,0,12">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <Grid Grid.Row="0" Margin="0,0,0,8">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding KeyName}" FontWeight="SemiBold" FontSize="13"/>
                                                    <Border Background="{Binding StatusColor}" CornerRadius="3" Padding="6,2" Margin="8,0,0,0">
                                                        <TextBlock Text="{Binding StatusText}" FontSize="10" Foreground="White"/>
                                                    </Border>
                                                </StackPanel>

                                                <Button Content="🗑️ 删除" Padding="8,4" HorizontalAlignment="Right"
                                                        Click="DeleteApiKey_Click" Tag="{Binding Id}"
                                                        Background="Transparent" BorderBrush="#D13438" 
                                                        Foreground="#D13438" BorderThickness="1" Cursor="Hand"/>
                                            </Grid>

                                            <Grid Grid.Row="1">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="{Binding MaskedKey}" FontFamily="Consolas" FontSize="12" Foreground="#666"/>
                                                    <TextBlock FontSize="11" Foreground="#999" Margin="0,4,0,0">
                                                        <Run Text="使用次数："/>
                                                        <Run Text="{Binding UsageCount}"/>
                                                        <Run Text=" | 今日："/>
                                                        <Run Text="{Binding TodayUsage}"/>
                                                        <Run Text="/"/>
                                                        <Run Text="{Binding DailyLimitDisplay}"/>
                                                    </TextBlock>
                                                </StackPanel>

                                                <CheckBox Grid.Column="1" Content="启用" IsChecked="{Binding IsActive}" 
                                                          VerticalAlignment="Center" Margin="12,0,0,0"/>
                                            </Grid>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- 高级设置 -->
                <Expander Header="⚙️ 高级设置（可选）" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,20">
                    <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6" Padding="20" Margin="0,12,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,12,16">
                                <TextBlock Text="Temperature (0-2)" Style="{StaticResource LabelStyle}"/>
                                <TextBox x:Name="TemperatureTextBox" Style="{StaticResource TextBoxStyle}" Text="0.7"/>
                            </StackPanel>

                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="12,0,0,16">
                                <TextBlock Text="Max Tokens" Style="{StaticResource LabelStyle}"/>
                                <TextBox x:Name="MaxTokensTextBox" Style="{StaticResource TextBoxStyle}" Text="2000"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,12,16">
                                <TextBlock Text="超时时间（秒）" Style="{StaticResource LabelStyle}"/>
                                <TextBox x:Name="TimeoutTextBox" Style="{StaticResource TextBoxStyle}" Text="60"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="12,0,0,16">
                                <TextBlock Text="重试次数" Style="{StaticResource LabelStyle}"/>
                                <TextBox x:Name="MaxRetriesTextBox" Style="{StaticResource TextBoxStyle}" Text="3"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,12,0">
                                <TextBlock Text="RPM 限制（次/分钟）" Style="{StaticResource LabelStyle}"/>
                                <TextBox x:Name="RpmLimitTextBox" Style="{StaticResource TextBoxStyle}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="1" Margin="12,0,0,0">
                                <TextBlock Text="优先级" Style="{StaticResource LabelStyle}"/>
                                <TextBox x:Name="PriorityTextBox" Style="{StaticResource TextBoxStyle}" Text="0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Expander>
            </StackPanel>
        </ScrollViewer>

        <!-- 底部按钮 -->
        <Border Grid.Row="2" Background="White" BorderThickness="0,1,0,0" BorderBrush="#E0E0E0" Padding="24,16">
            <Grid>
                <CheckBox x:Name="EnabledCheckBox" Content="启用此提供商" IsChecked="True" 
                          VerticalAlignment="Center" FontSize="13"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="测试连接" Padding="20,10" Margin="0,0,12,0" Click="Test_Click"
                            Background="White" BorderBrush="#0078D4" Foreground="#0078D4" 
                            BorderThickness="1" Cursor="Hand"/>
                    <Button Content="取消" Padding="20,10" Margin="0,0,12,0" Click="Cancel_Click"
                            Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand"/>
                    <Button Content="保存" Padding="20,10" Click="Save_Click"
                            Background="#0078D4" Foreground="White" BorderThickness="0" 
                            FontWeight="SemiBold" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
