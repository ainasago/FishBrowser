<Window x:Class="FishBrowser.WPF.Views.CreateGroupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="创建浏览器分组" 
        Width="500" 
        Height="400"
        WindowStartupLocation="CenterOwner"
        Background="#F5F7FA"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <StackPanel VerticalAlignment="Stretch">
            <!-- Title -->
            <TextBlock Text="🌐 创建新分组" FontSize="20" FontWeight="Bold" Foreground="#1F2937"/>
            <TextBlock Text="为不同场景创建浏览器分组，配置独立的指纹和规则" 
                       FontSize="12" Foreground="#6B7280" TextWrapping="Wrap"/>

            <Separator Background="#E5E7EB"/>

            <!-- Icon Selection -->
            <StackPanel Margin="0,16,0,0">
                <TextBlock Text="分组图标" FontSize="12" FontWeight="SemiBold" Foreground="#374151"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Name="IconButton1" Content="🌐" FontSize="24" Width="50" Height="50" 
                            Background="White" BorderBrush="#E5E7EB" BorderThickness="1"
                            Click="IconButton_Click" Tag="🌐" Margin="0,0,4,0"/>
                    <Button Name="IconButton2" Content="🛍️" FontSize="24" Width="50" Height="50" 
                            Background="White" BorderBrush="#E5E7EB" BorderThickness="1"
                            Click="IconButton_Click" Tag="🛍️" Margin="0,0,4,0"/>
                    <Button Name="IconButton3" Content="📱" FontSize="24" Width="50" Height="50" 
                            Background="White" BorderBrush="#E5E7EB" BorderThickness="1"
                            Click="IconButton_Click" Tag="📱" Margin="0,0,4,0"/>
                    <Button Name="IconButton4" Content="🔍" FontSize="24" Width="50" Height="50" 
                            Background="White" BorderBrush="#E5E7EB" BorderThickness="1"
                            Click="IconButton_Click" Tag="🔍" Margin="0,0,4,0"/>
                    <Button Name="IconButton5" Content="🎮" FontSize="24" Width="50" Height="50" 
                            Background="White" BorderBrush="#E5E7EB" BorderThickness="1"
                            Click="IconButton_Click" Tag="🎮"/>
                </StackPanel>
                <TextBlock Name="SelectedIconText" Text="已选择: 🌐" FontSize="11" Foreground="#6B7280"/>
            </StackPanel>

            <!-- Group Name -->
            <StackPanel>
                <TextBlock Text="分组名称 *" FontSize="12" FontWeight="SemiBold" Foreground="#374151"/>
                <TextBox Name="GroupNameInput" 
                         Background="White" 
                         BorderBrush="#D1D5DB" 
                         BorderThickness="1"
                         Margin="0,4,0,0"
                         Padding="12,10"
                         FontSize="12"
                         Foreground="#1F2937"/>
            </StackPanel>

            <!-- Description -->
            <StackPanel Margin="0,12,0,0">
                <TextBlock Text="分组描述" FontSize="12" FontWeight="SemiBold" Foreground="#374151"/>
                <TextBox Name="GroupDescriptionInput" 
                         Background="White" 
                         BorderBrush="#D1D5DB" 
                         BorderThickness="1"
                         Margin="0,4,0,0"
                         Padding="12,10"
                         FontSize="12"
                         Foreground="#1F2937"
                         Height="80"
                         TextWrapping="Wrap"
                         VerticalAlignment="Top"/>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" Margin="0,16,0,0" HorizontalAlignment="Right">
                <Button Name="CancelButton" Content="取消" 
                        Background="#F3F4F6" Foreground="#374151" 
                        Padding="20,10" FontSize="12" FontWeight="SemiBold" Margin="0,0,12,0"
                        Click="CancelButton_Click"
                        Width="100"/>
                <Button Name="CreateButton" Content="✓ 创建" 
                        Background="#3B82F6" Foreground="White" 
                        Padding="20,10" FontSize="12" FontWeight="SemiBold"
                        Click="CreateButton_Click"
                        Width="100"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
