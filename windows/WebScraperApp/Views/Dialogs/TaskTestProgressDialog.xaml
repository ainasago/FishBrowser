<Window x:Class="FishBrowser.WPF.Views.Dialogs.TaskTestProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="测试运行中" Height="700" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"
        ResizeMode="CanResize">
    
    <Window.Resources>
        <Style x:Key="LogItemStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Consolas, Courier New"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
        
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#BDBDBD"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#757575"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#616161"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题和状态 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock x:Name="TitleText" Text="🧪 测试运行中" 
                       FontSize="18" FontWeight="Bold" Foreground="#333"/>
            <TextBlock x:Name="TaskNameText" Text="任务名称" 
                       FontSize="14" Foreground="#666" Margin="0,4,0,0"/>
        </StackPanel>
        
        <!-- 进度条 -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock x:Name="StageText" Text="当前阶段：初始化" 
                       FontSize="13" Foreground="#444" Margin="0,0,0,8"/>
            <ProgressBar x:Name="ProgressBar" Height="24" 
                         Minimum="0" Maximum="100" Value="0"/>
            <TextBlock x:Name="ProgressText" Text="0%" 
                       FontSize="12" Foreground="#666" 
                       HorizontalAlignment="Center" Margin="0,4,0,0"/>
        </StackPanel>
        
        <!-- 日志和截图 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>
            
            <!-- 日志面板 -->
            <Border Grid.Column="0" Background="White" 
                    BorderBrush="#E0E0E0" BorderThickness="1" 
                    CornerRadius="4" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Border Grid.Row="0" Background="#F8F9FA" 
                            BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" 
                            Padding="12,8">
                        <TextBlock Text="📋 执行日志" FontWeight="Bold" 
                                   FontSize="13" Foreground="#333"/>
                    </Border>
                    
                    <ScrollViewer Grid.Row="1" 
                                  VerticalScrollBarVisibility="Auto"
                                  x:Name="LogScrollViewer">
                        <ItemsControl x:Name="LogList" Padding="8">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" 
                                               Style="{StaticResource LogItemStyle}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
            
            <GridSplitter Grid.Column="1" Width="5" 
                          HorizontalAlignment="Center" 
                          Background="Transparent"/>
            
            <!-- 截图预览 -->
            <Border Grid.Column="2" Background="White" 
                    BorderBrush="#E0E0E0" BorderThickness="1" 
                    CornerRadius="4" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Border Grid.Row="0" Background="#F8F9FA" 
                            BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" 
                            Padding="12,8">
                        <TextBlock Text="📸 实时截图" FontWeight="Bold" 
                                   FontSize="13" Foreground="#333"/>
                    </Border>
                    
                    <ScrollViewer Grid.Row="1" 
                                  HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Auto">
                        <Image x:Name="ScreenshotImage" 
                               Stretch="Uniform" Margin="8"/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
        
        <!-- 统计信息 -->
        <Border Grid.Row="3" Background="White" 
                BorderBrush="#E0E0E0" BorderThickness="1" 
                CornerRadius="4" Padding="16" Margin="0,20,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="已执行步骤" FontSize="11" 
                               Foreground="#666"/>
                    <TextBlock x:Name="StepsExecutedText" Text="0 / 0" 
                               FontSize="16" FontWeight="Bold" 
                               Foreground="#333" Margin="0,4,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1">
                    <TextBlock Text="已用时间" FontSize="11" 
                               Foreground="#666"/>
                    <TextBlock x:Name="ElapsedTimeText" Text="00:00" 
                               FontSize="16" FontWeight="Bold" 
                               Foreground="#333" Margin="0,4,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2">
                    <TextBlock Text="成功步骤" FontSize="11" 
                               Foreground="#666"/>
                    <TextBlock x:Name="SuccessStepsText" Text="0" 
                               FontSize="16" FontWeight="Bold" 
                               Foreground="#4CAF50" Margin="0,4,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="3">
                    <TextBlock Text="失败步骤" FontSize="11" 
                               Foreground="#666"/>
                    <TextBlock x:Name="FailedStepsText" Text="0" 
                               FontSize="16" FontWeight="Bold" 
                               Foreground="#F44336" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 操作按钮 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="StopButton" Content="⏹️ 停止" 
                    Width="100" Height="36" Margin="0,0,12,0"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Click="Stop_Click"/>
            <Button x:Name="CopyLogButton" Content="📋 复制日志" 
                    Width="120" Height="36" Margin="0,0,12,0"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Click="CopyLog_Click"/>
            <Button x:Name="CloseButton" Content="关闭" 
                    Width="100" Height="36" IsEnabled="False"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
