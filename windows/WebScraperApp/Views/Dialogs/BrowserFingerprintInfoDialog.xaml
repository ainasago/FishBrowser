<Window x:Class="FishBrowser.WPF.Views.Dialogs.BrowserFingerprintInfoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🔍 浏览器指纹信息" Width="900" Height="700"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="20">
            <!-- 标题 -->
            <TextBlock Text="🔍 浏览器指纹信息" FontSize="24" FontWeight="Bold" Foreground="#333" Margin="0,0,0,15"/>
            <TextBlock Text="当前浏览器的所有指纹特征（可复制）" FontSize="12" Foreground="#666" Margin="0,0,0,15"/>
            
            <!-- 按钮栏 -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <Button Content="📋 全选" Width="80" Click="SelectAll_Click" Background="#4CAF50" Foreground="White"/>
                <Button Content="📋 复制" Width="80" Margin="10,0,0,0" Click="Copy_Click" Background="#2196F3" Foreground="White"/>
                <Button Content="🔄 刷新" Width="80" Margin="10,0,0,0" Click="Refresh_Click" Background="#FF9800" Foreground="White"/>
                <Button Content="💾 导出" Width="80" Margin="10,0,0,0" Click="Export_Click" Background="#9C27B0" Foreground="White"/>
            </StackPanel>
            
            <!-- Tab 控件 -->
            <TabControl Height="500" Margin="0,0,0,15">
                <TabItem Header="📄 文本格式">
                    <TextBox x:Name="FingerprintTextBox" 
                             Background="White" 
                             Foreground="#333"
                             FontFamily="Consolas"
                             FontSize="12"
                             Padding="15"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             IsReadOnly="False"/>
                </TabItem>
                <TabItem Header="📋 JSON 格式">
                    <TextBox x:Name="FingerprintJsonTextBox" 
                             Background="White" 
                             Foreground="#333"
                             FontFamily="Consolas"
                             FontSize="11"
                             Padding="15"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             IsReadOnly="False"/>
                </TabItem>
            </TabControl>
            
            <!-- 底部按钮 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="关闭" Width="100" Click="Close_Click" Background="#757575" Foreground="White"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
