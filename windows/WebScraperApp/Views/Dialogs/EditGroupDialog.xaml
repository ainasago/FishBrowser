<Window x:Class="FishBrowser.WPF.Views.EditGroupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑浏览器分组" 
        Width="500" 
        Height="500"
        WindowStartupLocation="CenterOwner"
        Background="#F5F7FA"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel VerticalAlignment="Stretch">
                <!-- Title -->
                <TextBlock Text="✏️ 编辑分组" FontSize="20" FontWeight="Bold" Foreground="#1F2937"/>

                <Separator Background="#E5E7EB"/>

                <!-- Icon Selection -->
                <StackPanel Margin="0,16,0,0">
                    <TextBlock Text="分组图标" FontSize="12" FontWeight="SemiBold" Foreground="#374151"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Name="IconButton1" Content="🌐" FontSize="24" Width="50" Height="50" 
                                Background="White" BorderBrush="#E5E7EB" BorderThickness="1"
                                Click="IconButton_Click" Tag="🌐"/>
                        <Button Name="IconButton2" Content="🛍️" FontSize="24" Width="50" Height="50" 
                                Background="White" BorderBrush="#E5E7EB" BorderThickness="1" Margin="8,0,0,0"
                                Click="IconButton_Click" Tag="🛍️"/>
                        <Button Name="IconButton3" Content="📱" FontSize="24" Width="50" Height="50" 
                                Background="White" BorderBrush="#E5E7EB" BorderThickness="1" Margin="8,0,0,0"
                                Click="IconButton_Click" Tag="📱"/>
                        <Button Name="IconButton4" Content="🔍" FontSize="24" Width="50" Height="50" 
                                Background="White" BorderBrush="#E5E7EB" BorderThickness="1" Margin="8,0,0,0"
                                Click="IconButton_Click" Tag="🔍"/>
                        <Button Name="IconButton5" Content="🎮" FontSize="24" Width="50" Height="50" 
                                Background="White" BorderBrush="#E5E7EB" BorderThickness="1" Margin="8,0,0,0"
                                Click="IconButton_Click" Tag="🎮"/>
                    </StackPanel>
                </StackPanel>

                <!-- Group Name -->
                <StackPanel>
                    <TextBlock Text="分组名称 *" FontSize="12" FontWeight="SemiBold" Foreground="#374151"/>
                    <TextBox Name="GroupNameInput" 
                             Background="White" 
                             BorderBrush="#D1D5DB" 
                             BorderThickness="1"
                             Margin="0,4,0,0"
                             Padding="12,10"
                             FontSize="12"
                             Foreground="#1F2937"/>
                </StackPanel>

                <!-- Description -->
                <StackPanel Margin="0,12,0,0">
                    <TextBlock Text="分组描述" FontSize="12" FontWeight="SemiBold" Foreground="#374151"/>
                    <TextBox Name="GroupDescriptionInput" 
                             Background="White" 
                             BorderBrush="#D1D5DB" 
                             BorderThickness="1"
                             Margin="0,4,0,0"
                             Padding="12,10"
                             FontSize="12"
                             Foreground="#1F2937"
                             Height="80"
                             TextWrapping="Wrap"
                             VerticalAlignment="Top"/>
                </StackPanel>

                <!-- Validation Rules -->
                <Separator Background="#E5E7EB"/>
                <TextBlock Text="校验规则" FontSize="12" FontWeight="SemiBold" Foreground="#374151"/>

                <StackPanel Margin="0,8,0,0">
                    <TextBlock Text="最小真实性评分 (0-100)" FontSize="12" Foreground="#6B7280"/>
                    <Slider Name="MinRealisticScoreSlider" 
                            Minimum="0" Maximum="100" 
                            Background="White"
                            Foreground="#3B82F6"
                            Height="6"
                            Margin="0,4,0,0"/>
                    <TextBlock Name="MinRealisticScoreText" FontSize="11" Foreground="#6B7280"/>
                </StackPanel>

                <StackPanel Margin="0,8,0,0">
                    <TextBlock Text="最大 Cloudflare 风险评分 (0-100)" FontSize="12" Foreground="#6B7280"/>
                    <Slider Name="MaxCloudflareRiskSlider" 
                            Minimum="0" Maximum="100" 
                            Background="White"
                            Foreground="#EF4444"
                            Height="6"
                            Margin="0,4,0,0"/>
                    <TextBlock Name="MaxCloudflareRiskText" FontSize="11" Foreground="#6B7280"/>
                </StackPanel>

                <!-- Buttons -->
                <StackPanel Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Right">
                    <Button Name="CancelButton" Content="取消" 
                            Background="#F3F4F6" Foreground="#374151" 
                            Padding="20,10" FontSize="12" FontWeight="SemiBold" Margin="0,0,12,0"
                            Click="CancelButton_Click"
                            Width="100"/>
                    <Button Name="SaveButton" Content="✓ 保存" 
                            Background="#10B981" Foreground="White" 
                            Padding="20,10" FontSize="12" FontWeight="SemiBold"
                            Click="SaveButton_Click"
                            Width="100"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
