<Window x:Class="FishBrowser.WPF.Views.SelectProxiesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择代理" Height="520" Width="640" WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip" MinWidth="560" MinHeight="420">
  <DockPanel Margin="12" LastChildFill="True">
    <!-- 顶部工具栏先声明 -->
    <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,8">
      <TextBox x:Name="SearchBox" Width="260" Height="28" Margin="0,0,8,0"/>
      <Button Content="搜索" Width="80" Height="28" Click="Search_Click"/>
      <Button Content="全选" Width="80" Height="28" Margin="8,0,0,0" Click="SelectAll_Click"/>
      <Button Content="全不选" Width="80" Height="28" Margin="8,0,0,0" Click="SelectNone_Click"/>
    </StackPanel>
    <!-- DataGrid 作为最后一个子元素填充剩余空间 -->
    <!-- DataGrid 作为最后一个子元素填充剩余空间 -->
    <DataGrid x:Name="Grid" AutoGenerateColumns="False" SelectionMode="Extended" SelectionUnit="FullRow"
              CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="False" BorderThickness="1" BorderBrush="#E0E0E0"
              VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" EnableRowVirtualization="True" ScrollViewer.CanContentScroll="True">
      <DataGrid.Columns>
        <DataGridCheckBoxColumn Header="选择" Binding="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="60"/>
        <DataGridTextColumn Header="地址" Binding="{Binding Address}" Width="*"/>
        <DataGridTextColumn Header="端口" Binding="{Binding Port}" Width="80"/>
        <DataGridTextColumn Header="协议" Binding="{Binding Protocol}" Width="80"/>
        <DataGridTextColumn Header="标签" Binding="{Binding Label}" Width="160"/>
      </DataGrid.Columns>
    </DataGrid>
    <!-- 底部分页与按钮 -->
    <DockPanel DockPanel.Dock="Bottom" Margin="0,8,0,0">
      <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
        <Button Content="上一页" Width="80" Click="PrevPage_Click"/>
        <Button Content="下一页" Width="80" Margin="8,0,0,0" Click="NextPage_Click"/>
        <TextBlock x:Name="PageInfo" Margin="12,0,0,0" VerticalAlignment="Center"/>
      </StackPanel>
      <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
        <Button Content="确定" Width="90" Margin="8,0,0,0" IsDefault="True" Click="Ok_Click"/>
        <Button Content="取消" Width="90" Margin="8,0,0,0" IsCancel="True"/>
      </StackPanel>
    </DockPanel>
  </DockPanel>
</Window>
