<Window x:Class="FishBrowser.WPF.Views.PresetDiffWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="预设差异对比" Height="600" Width="1000"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize">
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
      <TextBlock Text="对比对象:" VerticalAlignment="Center"/>
      <ComboBox x:Name="CompareCombo" Width="300" Margin="8,0,0,0" SelectionChanged="CompareCombo_SelectionChanged"/>
    </StackPanel>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="10"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <GroupBox Header="源预设" Grid.Column="0">
        <TextBox x:Name="SourceBox" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Courier New" FontSize="11" IsReadOnly="True"/>
      </GroupBox>

      <GroupBox Header="对比预设" Grid.Column="2">
        <TextBox x:Name="CompareBox" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Courier New" FontSize="11" IsReadOnly="True"/>
      </GroupBox>
    </Grid>

    <DockPanel Grid.Row="2" Margin="0,10,0,0">
      <TextBlock x:Name="StatusText"/>
      <Button Content="关闭" Width="80" DockPanel.Dock="Right" Click="Close_Click"/>
    </DockPanel>
  </Grid>
</Window>
