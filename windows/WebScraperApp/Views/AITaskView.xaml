<Page x:Class="FishBrowser.WPF.Views.AITaskView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1400"
      Title="AI 任务">

    <Page.Resources>
        <!-- 用户消息气泡样式 -->
        <Style x:Key="UserMessageStyle" TargetType="Border">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="80,8,16,8"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="MaxWidth" Value="600"/>
        </Style>

        <!-- AI 消息气泡样式 -->
        <Style x:Key="AIMessageStyle" TargetType="Border">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="16,8,80,8"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="MaxWidth" Value="600"/>
        </Style>

        <!-- 系统消息样式 -->
        <Style x:Key="SystemMessageStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFF9E6"/>
            <Setter Property="BorderBrush" Value="#FFE5B3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="16,8"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>

        <!-- 快捷操作按钮样式 -->
        <Style x:Key="QuickActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Margin" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F8F9FA"/>
                                <Setter Property="BorderBrush" Value="#0078D4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 -->
        <Border Grid.Row="0" Background="#F8F9FA" BorderThickness="0,0,0,1" BorderBrush="#E0E0E0" Padding="20,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🤖" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="AI 任务助手" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock x:Name="StatusText" Text="就绪" FontSize="12" Foreground="#666" Margin="16,0,0,0" VerticalAlignment="Center"/>
                    
                    <!-- AI 提供商选择 -->
                    <TextBlock Text="AI 提供商：" FontSize="12" Foreground="#666" Margin="24,0,8,0" VerticalAlignment="Center"/>
                    <ComboBox x:Name="ProviderComboBox" Width="200" Padding="8,6" FontSize="12" 
                              SelectionChanged="ProviderComboBox_SelectionChanged"
                              BorderBrush="#E0E0E0" BorderThickness="1"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="📋 历史任务" Padding="12,6" Margin="0,0,8,0" Click="ShowHistory_Click" 
                            Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand"/>
                    <Button Content="🗑️ 清空对话" Padding="12,6" Margin="0,0,8,0" Click="ClearChat_Click"
                            Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand"/>
                    <Button Content="⚙️ 设置" Padding="12,6" Click="Settings_Click"
                            Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 聊天区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="360"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：对话历史 -->
            <Border Grid.Column="0" Background="White">
                <Grid>
                    <ScrollViewer x:Name="ChatScrollViewer" VerticalScrollBarVisibility="Auto" Padding="0,16,0,16">
                        <StackPanel x:Name="ChatPanel">
                            <!-- 欢迎消息 -->
                            <Border Style="{StaticResource SystemMessageStyle}">
                                <StackPanel>
                                    <TextBlock Text="👋 欢迎使用 AI 任务助手" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>
                                    <TextBlock TextWrapping="Wrap" Foreground="#666" FontSize="13">
                                        我可以帮你创建网页自动化任务流程。你可以用自然语言描述需求，我会生成符合 DSL 规范的任务脚本。
                                    </TextBlock>
                                </StackPanel>
                            </Border>

                            <!-- 快捷示例 -->
                            <Border Margin="16,16,16,8" Padding="16" Background="#FAFBFC" CornerRadius="8">
                                <StackPanel>
                                    <TextBlock Text="💡 快速开始 - 点击示例" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,12"/>
                                    <WrapPanel>
                                        <Button Style="{StaticResource QuickActionButtonStyle}" Click="QuickExample_Click" Tag="login">
                                            <StackPanel>
                                                <TextBlock Text="🔐 登录流程" FontWeight="SemiBold"/>
                                                <TextBlock Text="自动登录网站" FontSize="11" Foreground="#666"/>
                                            </StackPanel>
                                        </Button>
                                        <Button Style="{StaticResource QuickActionButtonStyle}" Click="QuickExample_Click" Tag="search">
                                            <StackPanel>
                                                <TextBlock Text="🔍 搜索抓取" FontWeight="SemiBold"/>
                                                <TextBlock Text="搜索并提取结果" FontSize="11" Foreground="#666"/>
                                            </StackPanel>
                                        </Button>
                                        <Button Style="{StaticResource QuickActionButtonStyle}" Click="QuickExample_Click" Tag="pagination">
                                            <StackPanel>
                                                <TextBlock Text="📄 翻页采集" FontWeight="SemiBold"/>
                                                <TextBlock Text="多页数据抓取" FontSize="11" Foreground="#666"/>
                                            </StackPanel>
                                        </Button>
                                        <Button Style="{StaticResource QuickActionButtonStyle}" Click="QuickExample_Click" Tag="form">
                                            <StackPanel>
                                                <TextBlock Text="📝 表单填写" FontWeight="SemiBold"/>
                                                <TextBlock Text="自动填写表单" FontSize="11" Foreground="#666"/>
                                            </StackPanel>
                                        </Button>
                                    </WrapPanel>
                                </StackPanel>
                            </Border>

                            <!-- 动态消息将添加在这里 -->
                        </StackPanel>
                    </ScrollViewer>

                    <!-- 空状态提示 -->
                    <StackPanel x:Name="EmptyState" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                        <TextBlock Text="💬" FontSize="48" HorizontalAlignment="Center" Opacity="0.3"/>
                        <TextBlock Text="开始对话" FontSize="16" Foreground="#999" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 右侧：任务预览与操作 -->
            <Border Grid.Column="1" Background="#F8F9FA" BorderThickness="1,0,0,0" BorderBrush="#E0E0E0" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 预览标题 -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,12">
                        <TextBlock Text="📝 任务预览" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <TextBlock Text="生成的 DSL 脚本将显示在这里" FontSize="12" Foreground="#666" TextWrapping="Wrap"/>
                    </StackPanel>

                    <!-- DSL 预览区（可编辑） -->
                    <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="12">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <TextBox x:Name="DslPreviewBox" 
                                     TextWrapping="Wrap" 
                                     AcceptsReturn="True"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     FontFamily="Consolas"
                                     FontSize="12"
                                     Foreground="#333"
                                     Text="# 等待 AI 生成任务脚本..."/>
                        </ScrollViewer>
                    </Border>

                    <!-- 操作按钮 -->
                    <StackPanel Grid.Row="2" Margin="0,12,0,0">
                        <Button Content="✅ 保存任务" Padding="0,10" Margin="0,0,0,8" Click="SaveTask_Click"
                                Background="#0078D4" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand"/>
                        <Button Content="▶️ 运行测试" Padding="0,10" Margin="0,0,0,8" Click="RunTest_Click"
                                Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand"/>
                        <Button Content="🔧 AI 脚本助手" Padding="0,10" Margin="0,0,0,8" Click="OpenDebugWorkbench_Click"
                                Background="#10A37F" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand"
                                ToolTip="打开可视化调试工作台，AI 辅助编写和调试脚本"/>
                        <Button Content="📋 复制脚本" Padding="0,10" Margin="0,0,0,8" Click="CopyDsl_Click"
                                Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand"/>
                        <Button Content="📥 导出 YAML" Padding="0,10" Click="ExportYaml_Click"
                                Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- 底部输入区 -->
        <Border Grid.Row="2" Background="White" BorderThickness="0,1,0,0" BorderBrush="#E0E0E0" Padding="20,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 输入框 -->
                <Border Grid.Column="0" Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="24" Padding="16,12">
                    <Grid>
                        <TextBox x:Name="InputBox" 
                                 BorderThickness="0" 
                                 Background="Transparent"
                                 FontSize="14"
                                 VerticalAlignment="Center"
                                 TextWrapping="Wrap"
                                 MaxHeight="120"
                                 AcceptsReturn="True"
                                 KeyDown="InputBox_KeyDown"/>
                        <TextBlock Text="描述你的任务需求，例如：登录 example.com 并搜索商品..." 
                                   Foreground="#999" 
                                   FontSize="14"
                                   IsHitTestVisible="False"
                                   VerticalAlignment="Center"
                                   Margin="4,0,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=InputBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>

                <!-- 发送按钮 -->
                <Button Grid.Column="1" Content="发送 ➤" Padding="24,12" Margin="12,0,0,0" Click="Send_Click"
                        Background="#0078D4" Foreground="White" BorderThickness="0" 
                        FontSize="14" FontWeight="SemiBold" Cursor="Hand"
                        VerticalAlignment="Center">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="24" 
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#005A9E"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Background" Value="#CCCCCC"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Page>
