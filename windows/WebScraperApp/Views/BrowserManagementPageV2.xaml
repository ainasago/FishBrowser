<Page x:Class="FishBrowser.WPF.Views.BrowserManagementPageV2"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="浏览器管理">
    <Page.Resources>
        <!-- Converters -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 -->
        <Border Grid.Row="0" Background="#F5F5F5" Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 搜索框 -->
                <Border Grid.Column="0" Background="White" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" Padding="8,6" MaxWidth="400" HorizontalAlignment="Left">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🔍" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox x:Name="SearchBox" Grid.Column="1" BorderThickness="0" Background="Transparent" 
                                 GotFocus="SearchBox_GotFocus" LostFocus="SearchBox_LostFocus"
                                 TextChanged="SearchBox_TextChanged"
                                 ToolTip="搜索浏览器名称、UA、分组"/>
                        <TextBlock x:Name="SearchPlaceholder" Grid.Column="1" Text="搜索浏览器..." Foreground="#999" IsHitTestVisible="False" VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- 右侧按钮 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="🪄 一键随机" Width="120" Height="36" Margin="0,0,8,0" 
                            Background="#FF6B35" Foreground="White" BorderThickness="0"
                            Click="CreateRandomBrowser_Click" FontWeight="Bold"
                            ToolTip="一键创建随机浏览器环境"/>
                    <Button Content="➕ 新建浏览器" Width="120" Height="36" Margin="0,0,8,0" 
                            Background="#4CAF50" Foreground="White" BorderThickness="0"
                            Click="NewEnvironment_Click" FontWeight="Bold"/>
                    <Button Content="🔄 刷新" Width="80" Height="36" Click="Refresh_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 统计面板 -->
        <Border Grid.Row="1" Background="White" Padding="16,12" BorderBrush="#EEE" BorderThickness="0,1">
            <StackPanel Orientation="Horizontal">
                <!-- 总数 -->
                <Border Background="#E3F2FD" Padding="16,8" Margin="0,0,12,0" CornerRadius="4">
                    <StackPanel>
                        <TextBlock Text="总数" Foreground="#666" FontSize="12"/>
                        <TextBlock x:Name="TotalCountText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#2196F3"/>
                    </StackPanel>
                </Border>

                <!-- 运行中 -->
                <Border Background="#E8F5E9" Padding="16,8" Margin="0,0,12,0" CornerRadius="4">
                    <StackPanel>
                        <TextBlock Text="运行中" Foreground="#666" FontSize="12"/>
                        <TextBlock x:Name="RunningCountText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#4CAF50"/>
                    </StackPanel>
                </Border>

                <!-- 分组数 -->
                <Border Background="#FFF3E0" Padding="16,8" Margin="0,0,12,0" CornerRadius="4">
                    <StackPanel>
                        <TextBlock Text="分组数" Foreground="#666" FontSize="12"/>
                        <TextBlock x:Name="GroupCountText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#FF9800"/>
                    </StackPanel>
                </Border>

                <!-- 视图切换 -->
                <Border Background="#F5F5F5" Padding="4" Margin="24,0,0,0" CornerRadius="4" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <RadioButton x:Name="CardViewRadio" Content="📇 卡片" IsChecked="True" Margin="4" 
                                     Checked="ViewMode_Changed" Style="{StaticResource {x:Type RadioButton}}"/>
                        <RadioButton x:Name="ListViewRadio" Content="📋 列表" Margin="4" 
                                     Checked="ViewMode_Changed" Style="{StaticResource {x:Type RadioButton}}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- 主内容区 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：分组树 -->
            <Border Grid.Column="0" Background="#FAFAFA" BorderBrush="#EEE" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 分组标题 -->
                    <Border Grid.Row="0" Background="#F5F5F5" Padding="12,8" BorderBrush="#EEE" BorderThickness="0,0,0,1">
                        <TextBlock Text="📁 分组" FontWeight="Bold" FontSize="14"/>
                    </Border>

                    <!-- 分组树 -->
                    <TreeView x:Name="GroupTree" Grid.Row="1" BorderThickness="0" Background="Transparent"
                              SelectedItemChanged="GroupTree_SelectionChanged">
                        <TreeView.ItemContainerStyle>
                            <Style TargetType="TreeViewItem">
                                <Setter Property="IsExpanded" Value="True"/>
                                <Setter Property="Padding" Value="4"/>
                            </Style>
                        </TreeView.ItemContainerStyle>
                    </TreeView>

                    <!-- 分组操作按钮 -->
                    <StackPanel Grid.Row="2" Margin="12" Orientation="Vertical">
                        <Button Content="➕ 新建分组" Click="NewGroup_Click" Margin="0,0,0,6"/>
                        <Button Content="✏️ 编辑分组" Click="EditGroup_Click" Margin="0,0,0,6"/>
                        <Button Content="🗑️ 删除分组" Click="DeleteGroup_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 右侧：浏览器列表 -->
            <Grid Grid.Column="1" Background="White">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 过滤器和排序 -->
                <Border Grid.Row="0" Background="#F9F9F9" Padding="16,12" BorderBrush="#EEE" BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- 第一行：标题 -->
                        <Grid Grid.Row="0" Margin="0,0,0,12">
                            <TextBlock x:Name="CurrentGroupTitle" Text="全部浏览器" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock x:Name="FilteredCountText" Text="显示 0 个" Foreground="#666" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                        </Grid>
                        
                        <!-- 第二行：过滤器 -->
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- 引擎过滤 -->
                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                <TextBlock Text="引擎" FontSize="11" Foreground="#666" Margin="0,0,0,4"/>
                                <ComboBox x:Name="EngineFilterComboBox" SelectionChanged="Filter_Changed">
                                    <ComboBoxItem Content="全部引擎" Tag="" IsSelected="True"/>
                                    <ComboBoxItem Content="UndetectedChrome" Tag="UndetectedChrome"/>
                                    <ComboBoxItem Content="Chromium" Tag="Chromium"/>
                                    <ComboBoxItem Content="Firefox" Tag="Firefox"/>
                                </ComboBox>
                            </StackPanel>
                            
                            <!-- 操作系统过滤 -->
                            <StackPanel Grid.Column="1" Margin="0,0,8,0">
                                <TextBlock Text="操作系统" FontSize="11" Foreground="#666" Margin="0,0,0,4"/>
                                <ComboBox x:Name="OSFilterComboBox" SelectionChanged="Filter_Changed">
                                    <ComboBoxItem Content="全部系统" Tag="" IsSelected="True"/>
                                    <ComboBoxItem Content="Windows" Tag="windows"/>
                                    <ComboBoxItem Content="MacOS" Tag="macos"/>
                                    <ComboBoxItem Content="Linux" Tag="linux"/>
                                    <ComboBoxItem Content="Android" Tag="android"/>
                                    <ComboBoxItem Content="iOS" Tag="ios"/>
                                </ComboBox>
                            </StackPanel>
                            
                            <!-- 占位 -->
                            <StackPanel Grid.Column="2"/>
                            
                            <!-- 排序 -->
                            <StackPanel Grid.Column="3">
                                <TextBlock Text="排序" FontSize="11" Foreground="#666" Margin="0,0,0,4"/>
                                <ComboBox x:Name="SortComboBox" SelectionChanged="Sort_Changed">
                                    <ComboBoxItem Content="最新创建" Tag="createdat-desc" IsSelected="True"/>
                                    <ComboBoxItem Content="最早创建" Tag="createdat-asc"/>
                                    <ComboBoxItem Content="名称 A-Z" Tag="name-asc"/>
                                    <ComboBoxItem Content="名称 Z-A" Tag="name-desc"/>
                                    <ComboBoxItem Content="启动次数↓" Tag="launchcount-desc"/>
                                    <ComboBoxItem Content="启动次数↑" Tag="launchcount-asc"/>
                                </ComboBox>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>

                <!-- 浏览器列表（卡片视图） -->
                <ScrollViewer Grid.Row="1" x:Name="CardViewScroll" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="BrowserCardList" Margin="16">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="320" Height="160" Background="White" BorderBrush="#DDD" BorderThickness="1" 
                                        Margin="0,0,16,16" CornerRadius="8" Padding="16"
                                        MouseEnter="BrowserCard_MouseEnter" MouseLeave="BrowserCard_MouseLeave">
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.1"/>
                                    </Border.Effect>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- 标题行 -->
                                        <Grid Grid.Row="0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="🌐" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Name}" FontWeight="Bold" FontSize="14" 
                                                       TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                            <CheckBox Grid.Column="2" IsChecked="{Binding IsSelected, Mode=TwoWay}" 
                                                      VerticalAlignment="Center" Checked="BrowserCard_Checked" Unchecked="BrowserCard_Unchecked"/>
                                        </Grid>

                                        <!-- 信息区 -->
                                        <StackPanel Grid.Row="1" Margin="0,12,0,0">
                                            <TextBlock Text="{Binding FingerprintProfile.UserAgent}" Foreground="#666" FontSize="11" 
                                                       TextTrimming="CharacterEllipsis" Margin="0,0,0,4"/>
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                                <TextBlock Text="🔧 " FontSize="11"/>
                                                <TextBlock Text="{Binding Engine}" FontSize="11" Foreground="#666" Margin="0,0,8,0"/>
                                                <TextBlock Text="💻 " FontSize="11"/>
                                                <TextBlock Text="{Binding OS}" FontSize="11" Foreground="#666"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🚀 启动 " FontSize="11" Foreground="#666"/>
                                                <TextBlock Text="{Binding LaunchCount}" FontSize="11" Foreground="#666"/>
                                                <TextBlock Text=" 次" FontSize="11" Foreground="#666"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- 操作按钮 -->
                                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button Content="▶️" Width="32" Height="28" Margin="0,0,4,0" 
                                                    Click="LaunchBrowser_Click" Tag="{Binding}"
                                                    ToolTip="启动浏览器" Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                                            <Button Content="✏️" Width="32" Height="28" Margin="0,0,4,0" 
                                                    Click="EditBrowser_Click" Tag="{Binding}"
                                                    ToolTip="编辑"/>
                                            <Button Content="🔍" Width="32" Height="28" Margin="0,0,4,0" 
                                                    Click="ViewFingerprint_Click" Tag="{Binding}"
                                                    ToolTip="查看指纹信息"/>
                                            <Button Content="⋮" Width="32" Height="28" 
                                                    Click="ShowBrowserMenu_Click" Tag="{Binding}"
                                                    ToolTip="更多操作"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- 浏览器列表（表格视图） -->
                <DataGrid x:Name="BrowserListGrid" Grid.Row="1" Visibility="Collapsed"
                          AutoGenerateColumns="False" IsReadOnly="True" 
                          SelectionMode="Extended" SelectionChanged="BrowserList_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Width="40" Binding="{Binding IsSelected, Mode=TwoWay}"/>
                        <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="140"/>
                        <DataGridTextColumn Header="指纹配置" Binding="{Binding FingerprintProfile.Name}" Width="120"/>
                        <DataGridTextColumn Header="User-Agent" Binding="{Binding FingerprintProfile.UserAgent}" Width="200"/>
                        <DataGridTextColumn Header="引擎" Binding="{Binding Engine}" Width="80"/>
                        <DataGridTextColumn Header="OS" Binding="{Binding OS}" Width="60"/>
                        <DataGridTextColumn Header="启动次数" Binding="{Binding LaunchCount}" Width="80"/>
                        <DataGridCheckBoxColumn Header="持久化" Binding="{Binding EnablePersistence, Mode=OneWay}" Width="60"/>
                        <DataGridTextColumn Header="创建时间" Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd}" Width="100"/>
                    </DataGrid.Columns>
                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="▶️ 启动" Click="LaunchBrowser_Click"/>
                            <MenuItem Header="✏️ 编辑" Click="EditBrowser_Click"/>
                            <MenuItem Header="🔍 查看指纹信息" Click="ViewFingerprint_Click"/>
                            <Separator/>
                            <MenuItem Header="🔄 更换指纹" Click="ChangeProfile_Click"/>
                            <MenuItem Header="📁 移动到分组" Click="MoveToGroup_Click"/>
                            <MenuItem Header="🗑️ 清除会话" Click="ClearSession_Click"/>
                            <Separator/>
                            <MenuItem Header="🗑️ 删除" Click="DeleteBrowser_Click"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                </DataGrid>

                <!-- 批量操作栏（选中时显示） -->
                <Border x:Name="BatchOperationBar" Grid.Row="2" Background="#FFF9C4" Padding="16,12" 
                        BorderBrush="#FBC02D" BorderThickness="0,1,0,0" Visibility="Collapsed">
                    <Grid>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="SelectedCountText" Text="已选中 0 个浏览器" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,16,0"/>
                            <Button Content="▶️ 批量启动" Width="100" Margin="0,0,8,0" Click="BatchLaunch_Click"/>
                            <Button Content="🔄 批量更换指纹" Width="120" Margin="0,0,8,0" Click="BatchChangeProfile_Click"/>
                            <Button Content="📁 批量移动" Width="100" Margin="0,0,8,0" Click="BatchMove_Click"/>
                            <Button Content="🗑️ 批量删除" Width="100" Margin="0,0,8,0" Click="BatchDelete_Click" Background="#F44336" Foreground="White"/>
                        </StackPanel>
                        <Button Content="✖️ 取消选择" HorizontalAlignment="Right" Click="ClearSelection_Click"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <!-- 测试工具栏 -->
        <Border Grid.Row="3" Background="White" Padding="16,12" BorderBrush="#EEE" BorderThickness="0,1,0,0">
            <StackPanel>
                <!-- 第一行：诊断工具 -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="🔧 诊断工具:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <Button Content="🔍 指纹对比" Width="100" Margin="0,0,8,0" 
                            Click="CompareFingerprints_Click" Background="#4CAF50" Foreground="White" 
                            ToolTip="对比真实浏览器和 Playwright 的所有指纹特征"/>
                    <Button Content="🛡️ Cloudflare 测试" Width="120" Margin="0,0,8,0" 
                            Click="LaunchMVP_Click" Background="#FF6B35" Foreground="White" 
                            ToolTip="启动能通过 Cloudflare 验证的测试浏览器（完整防检测配置）"/>
                    <Button Content="🦊 Firefox 测试" Width="100" Margin="0,0,8,0" 
                            Click="LaunchFirefox_Click" Background="#FF7139" Foreground="White" 
                            ToolTip="使用 Firefox 测试（可能绕过 TLS 检测）"/>
                    <Button Content="🤖 Undetected Chrome" Width="140" Margin="0,0,8,0" 
                            Click="LaunchUndetectedChrome_Click" Background="#4285F4" Foreground="White" 
                            ToolTip="使用 Selenium + UndetectedChromeDriver（真实 TLS 指纹，成功率 90-95%）"/>
                </StackPanel>

                <!-- 第二行：状态和提示 -->
                <Grid>
                    <TextBlock x:Name="StatusText" Text="就绪" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="💡 提示：右键浏览器卡片查看更多操作" Foreground="#666" FontSize="11" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Page>
